"""Placeholder so `pytest` has at least one passing test to collect while the
real suite is being built.

TODO(tests/unit): replace with real unit tests per docs/architecture.md
section 8, e.g.:
- test_score_lead.py: fixed-input cases for the scoring function.
- test_lookup_jurisdiction_rule.py: known + unknown country lookups.
- test_dedupe.py: normalization + hashing of email/phone.
- test_parse_enquiry.py: with a mocked ModelAdapter returning canned
  ExtractedFields.
- test_schemas.py: valid/invalid payloads against Plan, ExtractedFields,
  VerifierDecision.
"""


def test_placeholder() -> None:
    assert True
