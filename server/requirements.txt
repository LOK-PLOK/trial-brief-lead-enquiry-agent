# Runtime dependencies. Versions pinned to what was verified to install and
# import cleanly together (see "Verify backend imports" in the scaffolding
# notes) — bump deliberately, not accidentally, since reproducibility of the
# evaluation harness depends on a stable environment.

fastapi==0.141.1
uvicorn[standard]==0.52.0
pydantic==2.13.4
pydantic-settings==2.14.2
SQLAlchemy==2.0.51
httpx==0.28.1

# No provider SDK is pinned yet: no concrete ModelAdapter is implemented (see
# app/llm/factory.py), so there is nothing to import today. Add the relevant
# SDK here (e.g. `openai==...`) when a provider is actually implemented;
# Ollama needs no SDK regardless, it's called over plain HTTP (httpx).
