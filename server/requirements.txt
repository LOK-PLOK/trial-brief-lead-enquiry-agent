# Runtime dependencies. Versions pinned to what was verified to install and
# import cleanly together (see "Verify backend imports" in the scaffolding
# notes) — bump deliberately, not accidentally, since reproducibility of the
# evaluation harness depends on a stable environment.

fastapi==0.141.1
uvicorn[standard]==0.52.0
pydantic==2.13.4
pydantic-settings==2.14.2
SQLAlchemy==2.0.51
httpx==0.28.1

# No provider SDK is pinned: app/llm/openrouter_adapter.py (the one concrete
# ModelAdapter implemented so far, see app/llm/factory.py) talks to
# OpenRouter's OpenAI-compatible REST API directly over the `httpx` above --
# no separate SDK needed. Same would go for Ollama, called the same way.
# Add a real provider SDK here only if a future adapter actually needs one
# (e.g. an SDK-only provider with no plain HTTP surface).
